language: php
php:
  - 5.6
  - 5.5
  - 5.4
before_install:
  - composer self-update
  - composer install
script:
  - vendor/bin/peridot --grep *Spec.php spec
after_script:
  - vendor/bin/phake spec:coveralls
